export { biometric, BiometricActions } from './plugin';
export { biometricFallback } from './fallback';

export type { CheckAvailabilityResponse, AuthenticatePayload, AuthenticateResponse } from './types';
