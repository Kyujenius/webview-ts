{
  "name": "@example/iframe",
  "version": "0.0.1",
  "private": true,
  "description": "Iframe embed example: parent page as host, iframes as clients — no native code involved",
  "type": "module",
  "scripts": {
    "dev": "vp dev --port 5177",
    "build": "vp build",
    "preview": "vp preview",
    "type-check": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@webview-ts/core": "workspace:*",
    "@webview-ts/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  }
}
